export type NavLink = {
  label: string;
  href: string;
};

export type EventInfo = {
  label: string;
  value: string;
};

export type HeroContent = {
  titleTop: string;
  titleBottom: string;
  subtitle: string;
  cta: string;
  previewLabel: string;
};

